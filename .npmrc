# @vercel/speed-insights declare des peers optionnels (SvelteKit -> vite 8) qui entrent en
# conflit avec le vite 7 de vitest 3.x (epingle, cf. vitest.config.ts). Ces peers ne nous
# concernent pas : on ignore la resolution stricte des peers (comportement npm 6).
legacy-peer-deps=true
